# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc
*.lof
*.lot
%OUTDIR%/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Secrets
.env
.env.local
.env.*.local

# Agents
CLAUDE.md
.claude/

# Private / unredacted corpus material
data/Testung1DIVA.zip
data/legal_qa/raw/

# IRR unblinding keys (raters mustn't see judge verdicts)
data/irr/_*

# Manuscript + exposé — local working copy only, not collaborator-facing
# (amends 2026-04-21 repo-scope decision: parking spot is here, gitignored,
# rather than the vault — see Agent-Shared/decisions-log.md 2026-05-04)
/Overleaf/
